import App from "./App";
import BarcodeScan from "./BarcodeScan"

export const SCREENS_MAP = {
    App: {
        name: 'barkod.App',
        screen: App
    },
    BarcodeScan: {
        name: 'barkod.BarcodeScan',
        screen: BarcodeScan
    }
};